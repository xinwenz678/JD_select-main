# 成员 D 进度评估、协作清单与明日计划

> 本文件记录 2026-09-08 的阶段评估，已由 [成员 D 最终进度](d-progress-final.md) 更新；历史分数保留用于追踪增量。

日期：2026-09-08  
评估依据：`MVP_TASK_PLAN.md` 中 D1–D6 的完整交付标准，以及当前 `JD_select-main` 的实际代码、OpenAPI 和浏览器验证记录。

## 1. 结论

- 按最终完整交付口径：**13 / 25 点，约 52%**。
- 按今天“可以独立完成、不开展正式大联调”的阶段目标：约 **80%**。
- 已形成可演示的页面骨架：新建分析输入原型、Result、History、状态切换、真实健康检查。
- 当前真实后端 OpenAPI 只有 `GET /api/health`。简历解析、岗位匹配、保存和历史业务接口都尚未出现在这份代码中，因此不能把 mock 页面计为真实端到端完成。
- 当前 `JD_select-main` 是不含 `.git` 的下载目录，无法确认真实分支、提交、远程及其他成员最新进度。今天最重要的协作动作是取得带 Git 历史的共享仓库或每位成员可复现的分支/提交。

## 2. D1–D6 完成度

| ID | 满分 | 当前计分 | 完成度 | 已完成证据 | 仍缺什么 |
|---|---:|---:|---:|---|---|
| D1 页面信息架构和低保真原型 | 3 | **3.0** | 100% | `docs/user-flow.md`、`docs/ux-design.md`；新建/结果/历史三页结构、字段、按钮、状态和异常路径齐全 | 团队字段评审后做少量修订，不影响当前 D1 完成判断 |
| D2 JD 输入与提交状态 | 4 | **1.5** | 38% | 新建页有 JD 输入、空输入提示和内存状态；Result/History 有 Loading/Error/Empty 展示；健康检查有真实 loading、失败和重试 | “开始匹配”仍禁用；未调用真实匹配；缺少匹配请求的 loading、失败重试、在途锁、修正输入传递和成功跳转 |
| D3 结果页 | 6 | **5.0** | 83% | 总分、三项分数、匹配/缺失技能、建议、算法版本、评分说明、证据缺失提示全部实现；320–1440 宽度无横向溢出 | 仍使用 mock；真实 evidence/suggestions 类型和保存状态未接入；200% 缩放与完整辅助技术待验 |
| D4 历史列表与详情 | 3 | **2.0** | 67% | 最近列表、岗位、分数、时间、空态、错误态、按 `id` 打开详情、重排/刷新/前进后退均通过 mock 检查 | 未接 A 的真实列表/详情；真实 404、时间、分页/最近 10 条和重新分析原文待验证 |
| D5 端到端验收、Bug 清单、测试报告 | 4 | **1.5** | 38% | `docs/test-plan.md` 有 TC01–TC10；22 组 UI/健康检查通过；有浏览器 JSON 报告和截图 | 五个主业务场景均未真实执行；没有基于真实链路的 Bug 清单/关闭证据；严重 Bug 为 0 尚不能下结论 |
| D6 视频与最终交付 | 5 | **0** | 0% | 尚未进入正式制作阶段 | 演示脚本、录屏分工、字幕、成片、时长检查、最终目录核验均未完成 |
| **总计** | **25** | **13.0** | **52%** | 设计与静态结果体验基础已经完成 | 真实业务接入、验收和最终展示材料是后续主体 |

计分说明：D3/D4 的页面实现质量较高，但因为真实接口不存在，只计到“静态/模拟数据可演示”阶段；D5 不能用 22 组 UI 检查替代 TC01–TC05 的真实算法与保存链路。

## 3. 今天需要的组内协作

今天不做正式大联调，只做一次 **30–45 分钟的契约评审与材料收集**。目标是让明天能直接接线，而不是现场改接口。

### 3.1 先解决协作基础（A 或组长）

今天必须拿到以下任一项：

1. 带 `.git` 的团队共享仓库地址；或
2. A/B/C 各自明确的分支名与提交哈希；或
3. 保留完整提交历史的 Git bundle。

当前下载的 `JD_select-main` 没有 Git 元数据，无法安全判断谁的版本最新，也不能从这里直接合并到 main。请 A/组长同时提供：当前集成基线提交、运行命令、未合并模块和公共接口变更记录。

### 3.2 向 A 收集

- 历史列表与详情实际可运行的分支/提交，而不是只给设计文档。
- `GET /api/matches` 的真实响应样例：是否为 `items/total/limit/offset`，默认是否最近 10 条。
- `GET /api/matches/{id}` 的详情样例、404 错误样例、`id` 格式。
- `created_at` 是否为带时区 UTC；详情是否包含 `resume_text`、`jd_text`，供“修改后重新分析”。
- 保存由 `POST /api/matches` 完成还是另有接口；失败、超时和重试是否可能重复保存。
- 统一错误是否为 `error.code/message/details`。

D 今天给 A 的回馈：History 已经按字符串 `id` 设计；可以接 `items` 列表，但不会在没有契约时猜字段或调用写接口。

### 3.3 向 B 收集

- `POST /api/resumes/parse` 的实际请求、成功和空输入/失败样例。
- 教育、经历、项目、技能的键名与数组结构。
- 用户修正解析结果后，B 组件保存什么数据；是修改后的原文、结构化对象，还是两者都有。
- B 的页面/组件入口、分支和提交；哪些区域由 B 保持所有权，避免 D 覆盖。

D 今天需要与 B 定下一个页面交接点：B 完成“解析并确认”后，向 D 的 JD/提交区域提供一份明确的“有效简历输入”。如果结构尚未定，先记录决定，不在前端擅自加生产字段。

### 3.4 向 C 收集

- `POST /api/jobs/parse`、`POST /api/matches` 的真实请求、成功、校验失败和服务器失败样例。
- `job_title` 的来源；`score_breakdown` 三项是否整数及范围；总分是否等于分项和。
- `suggestions` 是字符串数组还是对象数组。
- 评分依据/evidence 的字段、原文摘录和定位结构。
- `algorithm_version` 的规则及 TC01–TC03 的期望区间。
- PyTorch/pytorch、JS/JavaScript 的归一化输出名称和重复计分规则。

D 今天给 C 的回馈：Result 已支持当前 mock 字段；未知字段会显示可读占位，不会在前端改分或伪造证据。

### 3.5 30–45 分钟评审的输出

会议只确认以下六件事：

1. 修正后的简历怎样进入匹配请求。
2. `POST /api/matches` 的最终请求/响应样例。
3. evidence 和 suggestions 的最终形状。
4. 匹配成功与自动保存的关系，以及重试是否会重复保存。
5. 历史列表/详情、id、时间和错误格式。
6. A/B/C/D 各自可运行分支、提交和明天可交付时间。

会议产出应是一份字段决定记录和三份可复制的 JSON：匹配成功、历史列表、历史详情/不存在错误。没有实现的字段标“待实现”，不要预填“已联调通过”。

## 4. 明日工作安排（9 月 9 日）

明天对应 MVP 计划的“纵向切片开发”。D 的静态页面已经提前完成，因此重点应从继续美化转为 **适配层、真实状态和一条可复现链路**。

| 时间 | D 的工作 | 依赖/协作 | 验收出口 |
|---|---|---|---|
| 09:00–09:30 | 取得真实 Git 基线，核对 A/B/C 提交和契约决定 | A/组长 | 能说明当前分支、基线哈希和待接模块；从真实基线建立 D 工作分支，不直接改 main |
| 09:30–10:30 | 把页面展示类型与 API 适配分开，保留 mock 开关 | A/C 契约样例 | Result/History 组件无需改业务布局即可接 mock 或真实数据；不把本地 display 类型冒充公共 schema |
| 10:30–12:00 | 优先接 A 的只读历史列表/详情 | A 提供可运行接口 | 空历史、列表、按 id 详情、404 和请求失败状态实际可演示；无写入副作用 |
| 13:30–14:30 | 接 B 的“解析确认 → 有效简历输入”交接点 | B 提供组件/schema | D 不重写解析；用户修正后数据不会在进入匹配前丢失 |
| 14:30–16:00 | 接 C 的匹配请求；完成 JD 校验、Loading、失败重试和在途防重 | C 提供可运行 `POST /api/matches` | 有效请求最多提交一次；成功进入 Result；失败保留输入并可重试；mock/真实来源清楚区分 |
| 16:00–17:00 | 运行 TC04、TC06–TC10；与 C 联合抽查 TC01–TC03 | A/B/C | 记录实际结果、截图/日志和 Bug；没有依赖的用例仍标阻塞，不预填通过 |
| 17:00–17:40 | 修 D 侧 P0/P1 UI 问题，回归构建和 320/390/1440 宽度 | 对应负责人确认接口缺陷 | 类型检查、构建、浏览器控制台通过；职责外问题进入 Bug 清单 |
| 17:40–18:00 | 更新测试报告、handoff 和演示脚本骨架 | A 复核 | 写清完成/未完成、提交哈希、启动命令和第二天接续点 |

### 明日优先级和降级方案

1. **最高优先级：真实 Git 基线和字段契约。** 没有这两项，不做盲目接线。
2. **第二优先级：真实匹配请求的 Loading/Error/重复提交。** 这是 D2 当前最大缺口。
3. **第三优先级：真实历史列表/详情。** 如果 A 接口先稳定，可先完成这条只读切片。
4. **第四优先级：真实高/中/低算法验收。** 需要 C 给明确样例和期望，D 负责执行、展示和记录，不改算法。
5. 如果 B/C 接口明天仍未可运行，D 保留当前 mock，完成 API 适配层、错误映射、测试夹具和 Bug/阻塞记录；不为了“跑通”在 D 分支实现解析或评分。

## 5. 明日完成标准

理想出口：浏览器中使用一份合成简历和 JD，经过已确认输入发起真实匹配，展示真实结果，并按服务端 `id` 在历史中重新打开；同时保留错误、空态和防重复提交。

最低可接受出口：

- 真正的 D 工作分支有明确基线；
- A/B/C 的字段决定有 JSON 样例和提交哈希；
- Result/History 已通过适配层准备好接真实数据；
- 至少真实接通一条只读历史链路或一条匹配链路；
- TC04、TC06–TC10 有实际记录或明确阻塞人/时间；
- 没有把 mock、健康检查或静态状态说成业务联调完成。

## 6. D6 的安排

D6 不建议明天投入大量录屏时间，因为真实业务链路尚未形成。明天只做 20 分钟演示脚本骨架：页面顺序、讲解人、样例、预计时长和故障回退。正式录屏、字幕、MP4 时长与交付目录核验安排在功能冻结和真实验收之后。
