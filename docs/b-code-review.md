# B 对整合候选的代码复核

日期：2026-09-10。用户已明确 B 直接修复 A 留下的问题。本轮因此同时包含复现、实现修正与独立回归。

## 已确认的实现

- 匹配请求使用用户确认后的技能集合；空集合不会回退到原文。人工新增技能的证据来源为 `confirmed_resume` 且 `line=null`。
- 确认技能通过词典规范名与同义词归一化；`python/sql` 与规范写法等价。
- 短英文技能在保留分隔符的原文上检查边界，修复顿号/空格后的 SQL、JS，同时保留 JavaScript≠Java、MySQL≠SQL、Spring Boot≠Spring。
- 请求长度在 HTTP schema 层拦截；无可匹配技能的 JD 返回可读 422，失败不写历史。
- `confirmed_name` 写入结果 JSON 快照，创建响应和 UUID 详情都能恢复，结果页可见。
- STAR 支持可选 OpenAI 兼容模型；无 Key、超时、网络、HTTP、JSON 或字段异常回退确定性规则，不影响 Level 1。建议不覆盖原文并保留人工核实提示。
- 保存失败事务回滚，失败请求不留下半条记录；历史最近 10 条与 UUID 详情在重启后保持一致。
- 前端有同步在途锁、请求状态、草稿保留、真实重试、过期响应隔离、非法 hash 恢复、刷新/复制/前后退和移动端布局。
- Vitest/Playwright 已更新，锁文件与 package.json 一致，audit 为 0。

## 复核限制

- 真实外部模型没有可用凭据，因此本轮通过注入兼容响应和超时异常验证两个代码分支；规则回退由真实 HTTP/E2E 验证。接入实际供应商时还需用小组自己的 Key 做一次脱敏 smoke。
- 当前文件夹没有 `.git`。源码差异、提交作者、分支、origin/main 和候选哈希无法从本目录验证。
- 同机干净安装已完成，另一台电脑从 GitHub 克隆仍依赖可访问仓库和候选提交。

日志隐私检查覆盖了故障注入路径：响应和应用错误日志没有完整简历、JD、测试秘密、SQL 参数或栈。此结论不等同于完整安全审计。
