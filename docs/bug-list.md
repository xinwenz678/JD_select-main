# 候选缺陷清单

更新：2026-09-10。对象：`JD_select-main` 本地整合版本。A 交付后发现的产品缺陷已由 B 直接修复并复验；当前没有未解决的 P0/P1 产品缺陷。

| ID | 原等级 | 原问题 | 修复与证据 | 状态 |
|---|---:|---|---|---|
| RC-B01 | P1 | 顿号分隔的 SQL/JS 漏识别 | 短英文词改为在保留分隔符的原文上匹配，同时保留 MySQL≠SQL、JavaScript≠Java、Spring Boot≠Spring；规则边界及五场景通过 | 已关闭 |
| RC-B02 | P1 | `python/sql` 与 `Python/SQL` 确认技能得分不同 | 新增规范名与同义词统一映射；别名等价用例通过 | 已关闭 |
| RC-B03 | P1 | 无已知技能 JD 返回 500 | 匹配入口返回可读 422，失败不保存记录 | 已关闭 |
| RC-B04 | P1 | 超长简历/JD/岗位名返回 500 | 请求 schema 与存储边界对齐为 100000/100000/200，超限 422 且不保存 | 已关闭 |
| RC-B05 | 发布风险 | Vitest/Playwright 开发依赖存在审计告警 | 升级到 Vitest 4.1.11、Playwright 1.63.0并重建锁；测试、构建通过，`npm audit` 0 项 | 已关闭 |
| RC-B06 | 范围缺口 | STAR 只有规则服务 | 增加 OpenAI 兼容 `/chat/completions` 调用；无配置、超时、HTTP/格式异常统一回退规则；成功与超时分支自动测试通过 | 已关闭 |
| RC-B07 | P2 | `confirmed_name` 接收后未使用或持久化 | 姓名写入结果快照、详情恢复，并在结果页显示 | 已关闭 |
| RC-B08 | 交付依赖 | 当前目录没有 `.git`，无法核对 main/origin/main/提交哈希 | 需要仓库维护者把本地候选放入真实 Git 工作区后再运行 Git 门禁 | 外部待办 |

完整门禁：后端 110/110，组件 12/12，真实浏览器 17/17，规则边界、构建、官方 verify、启动 smoke 全部通过。唯一警告是测试依赖 Starlette 对 AnyIO 类型别名的弃用提示，不影响运行，也不是当前 P1。

证据位于 `docs/evidence/b-candidate/`。其中 UUID 和输入均来自隔离临时数据库中的合成数据，不代表真实招聘结果。
