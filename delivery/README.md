# 最终交付暂存区

当前目录用于集中最终交付材料，尚未达到可提交状态。

- `source/`：最终源代码压缩包。
- `video/`：最终 MP4，必须少于 10 分钟。
- `reports/`：四份独立个人报告。
- `evidence/`：脱敏截图、最终测试报告与必要日志。

不要放 `.env`、API Key、真实简历、手机号、邮箱、数据库、虚拟环境或 `node_modules`。详细核验步骤见 `docs/delivery-checklist.md`。当前只有本说明，避免用空白占位文件冒充最终成果。

