# 成员 D Bug 与阻塞清单

更新时间：2026-09-09。

严重度：P0 阻断启动/主流程/数据安全；P1 主要功能或误导；P2 低频体验或验证缺口。`DEPENDENCY` 表示缺少其他模块，不等同于已实现代码中的缺陷。

## 1. 当前开放项

| ID | 类型 | 严重度 | 问题 | 影响 | 负责人/依赖 | 复验条件 |
|---|---|---|---|---|---|---|
| DEP-001 | DEPENDENCY | P0 | 当前 OpenAPI 只有 `/api/health` | 不能进行真实解析、匹配、保存和历史验收 | A/B/C | 对应分支可运行，OpenAPI 出现确认后的接口 |
| DEP-002 | DEPENDENCY | P0 | 当前不是有效 Git 仓库；父目录只有空 `.git` 文件夹 | 无法确认基线、提交和安全合并路径 | A/组长 | 提供共享仓库、分支哈希或 Git bundle，且 `git status` 可运行 |
| INT-001 | 契约待定 | P1 | 人工修正后的简历怎样进入匹配请求未确定 | 可能丢失用户修正或擅自扩展公共字段 | A/B/C/D | 形成请求样例并由接口两端确认 |
| INT-002 | 契约待定 | P1 | evidence、suggestions、历史列表/详情与错误形状待确认 | D 页面只能展示占位，不能安全适配真实数据 | A/C/D | 成功、失败、列表和详情 JSON 样例确认 |
| INT-003 | 契约待定 | P1 | 匹配成功、自动保存、超时重试与重复保存语义待确认 | 真实重试可能生成重复历史记录 | A/C | 明确事务和幂等策略并完成专用测试 |
| QA-001 | 验证缺口 | P2 | 已通过 200% 缩放等效视口，尚未在目标演示浏览器手工缩放并完成屏幕阅读器检查 | 可访问性结论仍有限 | D | 在目标演示浏览器手工执行并记录 |
| DEL-001 | 交付依赖 | P1 | 最终 MP4、四份个人报告和源代码压缩包尚未形成 | D6 最终验收不能完成 | 全员，D 统筹 | 全部放入 delivery/，运行预检并人工确认视频 |

## 2. 已关闭或已验证项

| ID | 原问题 | 处理 | 复验结果 |
|---|---|---|---|
| D-UI-001 | Result/History 缺少静态业务页面 | 实现三页统一导航、Result、History 和状态组件 | 构建和浏览器检查通过 |
| D-UI-002 | 缺字段、0 分、空数组和非法时间容易混淆 | 增加独立占位规则和 `demo-incomplete` 测试记录 | 自动化通过，0 与缺失明确区分 |
| D-UI-003 | JD 提交状态只有禁用按钮 | 增加明确标注的 mock 提交状态机、空校验、Loading、失败、重试和在途锁 | 自动化通过；无业务 API 请求 |
| D-UI-004 | 列表重排可能用数组下标打开错误详情 | 使用记录 `id` 作为 key 与导航标识 | 重排、刷新、前进/后退复验通过 |
| D-QA-001 | 初次精确文本检查被装饰图标干扰 | 分离图标和提示文案节点 | 重跑通过 |

## 3. 当前质量判断

- D 侧 mock 页面验证范围内，没有未关闭的 P0/P1 UI 缺陷。
- DEP-001/DEP-002 是当前真实 MVP 的主要阻塞，不能写成 D 已解决。
- 正式联调后需要重新建立 Bug 清单；目前不能宣布整个系统严重 Bug 为 0。
