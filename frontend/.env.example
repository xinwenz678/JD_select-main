# Browser-visible configuration only. Never put secrets in VITE_* variables.
# Same-origin /api requests are proxied to FastAPI during development.
VITE_API_BASE_URL=/api
# Development server proxy target (not exposed as a VITE_* variable).
API_PROXY_TARGET=http://127.0.0.1:8000
