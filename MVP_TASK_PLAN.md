# AI 简历诊断与岗位匹配系统：四人 MVP 任务框架

## 1. 项目定位

### 1.1 当前判断与边界

结合项目目录名 `JD_select`、已有简历样例以及课程选题要求，本项目按 T5“AI 简历诊断与岗位匹配系统”规划。

MVP 只承诺完成一条稳定、可演示、可复现的核心链路：

> 用户粘贴简历文本 → 系统解析基本信息与技能 → 用户粘贴岗位 JD → 系统提取岗位技能 → 计算可解释匹配分 → 展示匹配项、缺失项和改进建议 → 保存分析记录。

### 1.2 MVP 必须完成（P0）

1. 简历文本粘贴、编辑与基础校验。
2. 简历结构化解析：姓名/联系方式可选，教育、经历、项目、技能至少能分段识别。
3. JD 文本输入、编辑与基础校验。
4. 简历和 JD 的技能关键词提取，至少覆盖 40 个常见技能词及同义词。
5. 可解释匹配结果：总分、匹配技能、缺失技能、评分依据。
6. 基于规则的改进建议：缺失关键词提醒、经历缺量化成果提醒、弱动词提醒。
7. 分析记录保存与查看；至少可查看最近 10 条。
8. 一键运行说明、示例数据、自动化测试和完整演示脚本。

### 1.3 有余力再做（P1）

1. 接入大模型，对经历描述给出 STAR 改写建议。
2. 上传 `.txt`/`.docx` 简历并解析。
3. 多份 JD 横向对比、匹配结果排序。
4. 结果导出为 PDF 或 Markdown。

### 1.4 本轮明确不做（P2）

1. 爬取招聘网站或绕过登录/反爬机制。
2. 复杂账号、权限、支付、消息系统。
3. 模型训练、微调和不可解释的黑盒评分。
4. Level 3 就业市场分析智能体、薪资分析和技能词云。
5. 生产级高并发、分布式任务与微服务拆分。

## 2. 用户流程与验收场景

### 2.1 主流程

1. 用户进入“新建分析”页。
2. 粘贴简历文本，点击“解析简历”。
3. 系统显示解析出的区块和技能，用户可修正。
4. 用户粘贴 JD，点击“开始匹配”。
5. 系统展示 0-100 分总分、分项分、匹配技能、缺失技能和改进建议。
6. 用户查看评分说明，确认每一项分数可以追溯到文本证据。
7. 系统保存本次分析；用户可在“历史记录”中再次打开。

### 2.2 演示时必须通过的 5 个场景

| 场景 | 输入 | 预期结果 |
|---|---|---|
| 完整匹配 | Python/FastAPI/SQL/数据分析简历 + 对应 JD | 高分，匹配项完整，缺失项较少 |
| 部分匹配 | 只有 Python/Excel 的简历 + 后端 JD | 中等分，明确列出 FastAPI/SQL 等差距 |
| 低匹配 | 市场运营简历 + Python 后端 JD | 低分，不得给出虚假的匹配技能 |
| 空输入 | 空简历或空 JD | 前端阻止提交，后端返回 422/400 和可读错误 |
| 同义词 | “PyTorch”/“pytorch”、“JS”/“JavaScript” | 归一化后视为同一技能 |

## 3. 推荐技术方案

为保证 5-6 天内可完成，建议采用模块化单体：

- 前端：React + Vite + TypeScript + Tailwind CSS。
- 后端：FastAPI + Pydantic + SQLAlchemy。
- 数据库：SQLite 作为默认演示环境；保留 `DATABASE_URL`，可切换 PostgreSQL。
- 匹配算法：技能词典 + 同义词归一化 + 加权集合匹配；不依赖外部 API。
- 可选 AI：OpenAI/其他兼容接口，仅用于 STAR 建议；无 API Key 时自动使用规则建议。
- 测试：后端 Pytest，前端 Vitest，端到端至少保留一条可手工复现的验收脚本。
- 交付：提供 Windows PowerShell 启动命令；若时间允许，再补 Docker Compose。

### 3.1 建议目录

```text
JD_select/
├── frontend/
│   ├── src/pages/
│   ├── src/components/
│   ├── src/services/
│   └── tests/
├── backend/
│   ├── app/api/
│   ├── app/core/
│   ├── app/models/
│   ├── app/schemas/
│   ├── app/services/
│   ├── app/data/skill_dictionary.json
│   └── tests/
├── sample_data/
├── docs/
│   ├── requirements.md
│   ├── architecture.md
│   ├── api.md
│   ├── test-report.md
│   ├── runbook.md
│   └── demo-script.md
├── .env.example
├── README.md
└── handoff.md
```

## 4. 先冻结的公共契约

四人开始编码前必须共同确认以下契约。冻结后如需修改，必须在群内说明影响并由接口两端共同确认。

### 4.1 API 最小集合

| 方法 | 路径 | 用途 | 负责人 |
|---|---|---|---|
| `GET` | `/api/health` | 健康检查、版本信息 | 成员 A |
| `POST` | `/api/resumes/parse` | 解析简历文本 | 成员 B |
| `POST` | `/api/jobs/parse` | 解析 JD 与技能 | 成员 C |
| `POST` | `/api/matches` | 计算并保存匹配结果 | 成员 C，A 配合持久化 |
| `GET` | `/api/matches` | 最近分析列表 | 成员 A |
| `GET` | `/api/matches/{id}` | 分析详情 | 成员 A |
| `POST` | `/api/suggestions/star` | 可选 STAR 建议 | 成员 B；P1 才实现 |

### 4.2 核心请求与响应

`POST /api/matches` 请求：

```json
{
  "resume_text": "...",
  "jd_text": "...",
  "job_title": "数据分析实习生"
}
```

响应：

```json
{
  "id": "uuid",
  "score": 72,
  "score_breakdown": {
    "required_skills": 55,
    "preferred_skills": 12,
    "evidence_quality": 5
  },
  "matched_skills": ["Python", "SQL"],
  "missing_skills": ["Tableau"],
  "resume_sections": {},
  "job_skills": [],
  "suggestions": [],
  "algorithm_version": "rule-v1"
}
```

### 4.3 评分规则 v1

总分必须可解释且稳定，建议使用：

- 必需技能覆盖率：60 分。
- 加分技能覆盖率：20 分。
- 经历证据质量：20 分，其中量化成果 10 分、动作动词 5 分、项目/实习证据 5 分。
- 总分限制在 0-100，计算过程返回 `score_breakdown`。
- 技能词未命中不得由 AI 猜测为已掌握；结果页标明“匹配分用于辅助判断，不代表录用概率”。

### 4.4 数据模型

`analysis_records` 最少包含：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | UUID/Text | 主键 |
| `job_title` | Text | 岗位名，可为空 |
| `resume_text` | Text | 演示环境原文 |
| `jd_text` | Text | JD 原文 |
| `score` | Integer | 总分 |
| `result_json` | JSON/Text | 分项及建议 |
| `algorithm_version` | Text | 算法版本 |
| `created_at` | DateTime | 创建时间 |

隐私约束：不把简历或 API Key 提交到公开仓库；示例数据必须脱敏；日志不得输出完整简历、手机号和邮箱。

## 5. 四人均分方案

以每人 25 个任务点规划。每个人都同时承担“开发、测试、文档、演示”四类工作，避免角色失衡。成员姓名确定后，将 A-D 替换为真实姓名。

### 成员 A：平台、数据与集成负责人（25 点）

| ID | 任务 | 点数 | 产出/验收 |
|---|---|---:|---|
| A1 | 初始化仓库、前后端目录、环境变量模板 | 4 | 新机器按 README 可安装依赖 |
| A2 | FastAPI 基础、错误格式、健康检查、CORS | 4 | `/api/health` 返回版本与状态 |
| A3 | SQLAlchemy 数据模型、SQLite 初始化与记录 CRUD | 6 | 分析记录可新增、查询列表和详情 |
| A4 | 串联 B/C/D 模块，维护 API 契约和集成分支 | 4 | 主流程接口无字段冲突 |
| A5 | 启动脚本、依赖锁定、`.env.example`、运行说明 | 4 | 清空环境后可按文档启动 |
| A6 | 集成测试、异常日志检查、演示中的架构讲解 | 3 | 至少 4 个 API 集成测试通过 |

### 成员 B：简历解析与诊断负责人（25 点）

| ID | 任务 | 点数 | 产出/验收 |
|---|---|---:|---|
| B1 | 定义简历结构化 schema 和解析规则 | 4 | 能识别教育/经历/项目/技能区块 |
| B2 | 实现 `/api/resumes/parse` | 5 | 对 3 份样例输出稳定 JSON |
| B3 | 实现简历输入、编辑、解析结果确认界面 | 6 | 粘贴、校验、编辑、确认链路可用 |
| B4 | 实现规则诊断：量化成果、动作动词、缺失区块 | 4 | 建议能定位到对应区块 |
| B5 | 单元测试和 3 份脱敏简历样例 | 3 | 正常、缺区块、空文本均覆盖 |
| B6 | 编写简历模块说明，准备原始/优化简历对比演示 | 3 | 文档可直接用于报告和视频 |

### 成员 C：JD 解析与匹配算法负责人（25 点）

| ID | 任务 | 点数 | 产出/验收 |
|---|---|---:|---|
| C1 | 建立技能词典、同义词表和岗位样例集 | 5 | ≥40 个技能词，≥5 份真实/脱敏 JD |
| C2 | 实现 JD 解析和必需/加分技能识别 | 4 | 5 份 JD 的人工抽查结果合理 |
| C3 | 实现评分、分项分、差距清单和证据定位 | 7 | 同输入结果确定；0-100；可解释 |
| C4 | 实现 `/api/jobs/parse` 与 `/api/matches` 服务层 | 3 | 符合冻结的请求/响应契约 |
| C5 | 算法测试与边界测试 | 4 | 完整/部分/低匹配、大小写、同义词均覆盖 |
| C6 | 编写算法说明，准备技术亮点讲解 | 2 | 报告含公式、示例、局限性 |

### 成员 D：结果体验、质量与展示负责人（25 点）

| ID | 任务 | 点数 | 产出/验收 |
|---|---|---:|---|
| D1 | 完成页面信息架构和低保真原型 | 3 | 新建分析、结果、历史三页流程清楚 |
| D2 | 实现 JD 输入与提交状态 | 4 | 加载、失败、重试和空态完整 |
| D3 | 实现结果页：总分、分项、匹配/缺失技能、建议 | 6 | 手机/电脑宽度均可读，无截断 |
| D4 | 实现历史列表与详情页 | 3 | 最近 10 条可打开，空列表有提示 |
| D5 | 端到端验收、Bug 清单、测试报告 | 4 | 5 个主场景通过，严重 Bug 为 0 |
| D6 | 视频脚本、录屏统筹、字幕与最终交付目录核验 | 5 | MP4 <10 分钟，命名和目录符合要求 |

### 5.1 负载复核

| 成员 | 功能/代码 | 测试/质量 | 文档/演示 | 总点数 |
|---|---:|---:|---:|---:|
| A | 18 | 4 | 3 | 25 |
| B | 19 | 3 | 3 | 25 |
| C | 19 | 4 | 2 | 25 |
| D | 16 | 4 | 5 | 25 |

D 的视频和验收工作集中在最后两天，因此前两天应优先完成原型和结果组件；A 的集成工作贯穿全程，不应同时承担组长的全部材料收集工作。

## 6. 六天执行流程

### 第 1 天：立项与规则统一（9 月 7 日）

全员共同完成：

1. 确认组长、成员 A-D 映射和唯一沟通群。
2. 确认 MVP/P1/P2 边界，不在开发中临时增加账号、爬虫等功能。
3. 选取 5 份 JD 与 3 份脱敏简历，完成竞品体验分工。
4. 冻结技术栈、目录、Git 分支、接口格式和评分规则 v1。
5. 保存 AI 对话、提示词、生成内容和人工修订说明，作为个人报告证据。

出口条件：一页项目章程、任务认领表、风险清单、可访问的共享仓库。

### 第 2 天：需求与设计（9 月 8 日）

- A：项目脚手架、接口错误规范、数据模型初稿。
- B：简历 schema、3 份样例和解析规则。
- C：JD 标注、技能词典、评分公式样例计算。
- D：用户流程、低保真原型、验收场景。
- 16:00 进行 30 分钟评审：逐字段过 API、逐页面过流程、逐用例过预期。

出口条件：`requirements.md`、用例图、页面原型、ER 图、API 契约 v1、测试计划。

### 第 3 天：纵向切片开发（9 月 9 日）

- 上午：A 提供可启动后端；B/C 提供纯函数和接口 mock；D 提供可交互静态页面。
- 下午：打通第一条“假数据端到端”链路，再把 mock 替换为真实解析与评分。
- 当日结束前合并一次集成版本并打 `mvp-alpha` 标签。

出口条件：浏览器中能输入两段文本并看到一个真实计算出的结果。

### 第 4 天：功能完成与持续测试（9 月 10 日）

- B/C 完成解析、诊断、匹配和单元测试。
- A 完成记录持久化、历史接口、配置与异常处理。
- D 完成结果页、历史页、响应式和错误状态。
- 15:00 起联调；只修 P0，新的想法进入 P1 清单。

出口条件：所有 P0 功能完成，自动化测试通过，严重 Bug 清零。

### 第 5 天：冻结、验收与材料（9 月 11 日）

1. 10:00 功能冻结，之后不改接口、不重构、不换框架。
2. 在一台“未参与开发”的电脑上照 README 冷启动。
3. 执行 5 个演示场景并保存截图、测试结果和 Bug 关闭证据。
4. 完成架构图、ER 图、用例图、接口表和算法说明。
5. 每人基于自己的日志写个人报告，不复制同一段实施与反思内容。
6. 按脚本试录一次，控制在 8-9 分钟，为故障留余量。

出口条件：`mvp-rc1`、测试报告、运行说明、个人报告初稿、试录视频。

### 第 6 天：最终提交与答辩（9 月 12 日）

1. 只修阻断启动或演示的缺陷。
2. 最终录屏并检查声音、字幕、分辨率、时长和隐私信息。
3. 按命名规范生成压缩包、MP4 和 4 份个人报告。
4. 另一位成员交叉核验文件可打开、压缩包可解压、代码可运行。
5. 11:00 前完成内部提交，给正式 12:00 截止留出缓冲。

出口条件：最终交付包、校验清单、备份副本。

## 7. 协作流程

### 7.1 Git 规则

- 分支：`main` 只放可运行版本；个人分支使用 `feat/A-platform`、`feat/B-resume` 等。
- 提交：一次提交只解决一类问题，如 `feat(match): add weighted skill scoring`。
- 合并：至少一名非作者检查；涉及公共 schema 时必须由接口另一端检查。
- 每天至少两次集成：午间一次、下班前一次；避免最后一天一次性合并。
- 禁止提交 `.env`、API Key、真实手机号、真实完整简历和数据库文件。

### 7.2 每日短会模板（15 分钟）

每人只回答：

1. 昨天完成了哪个可验证结果？
2. 今天结束前要交付什么？
3. 当前阻塞是什么，由谁在几点前协助？
4. 是否修改公共接口、数据结构或演示流程？

### 7.3 Bug 优先级

- P0：无法启动、主流程中断、数据泄露、分数明显错误。立即修复，未清零不得交付。
- P1：主要页面错误、历史记录失效、结果表达误导。功能冻结前清零。
- P2：样式细节和低频体验问题。时间不足可记录在“展望”。

## 8. 完成定义（Definition of Done）

单项任务只有同时满足以下条件才算完成：

1. 代码已提交并通过另一名成员检查。
2. 正常路径和至少一个异常路径有测试或明确复现步骤。
3. 不包含真实隐私数据和明文密钥。
4. 相关 README/API/算法文档已更新。
5. 能从 UI 或接口实际演示，不以截图代替功能。
6. 对外输出有清晰错误提示，后端无未处理异常。

MVP 整体完成标准：

- 全新环境按运行说明在 15 分钟内启动。
- 5 个验收场景全部通过。
- 后端测试全部通过；前端关键计算和交互无控制台错误。
- 无 P0/P1 未关闭缺陷。
- 无外部 AI Key 时核心匹配仍可运行。
- 10 分钟内完成背景、架构、核心功能、技术亮点和实现思路演示。

## 9. 文档与交付责任

### 9.1 小组共同材料

| 材料 | 主笔 | 复核 |
|---|---|---|
| README/运行说明/常见问题 | A | D |
| 需求规格、用例图、简历模块说明 | B | D |
| 算法、数据字典、接口中的匹配部分 | C | A |
| 测试报告、视频脚本、交付清单 | D | A |
| 架构图与 ER 图 | A | B、C |

### 9.2 每人个人报告应保留的独立证据

1. 自己负责模块的需求与设计决策。
2. 至少两个真实技术问题及排查过程。
3. AI 在需求、设计、编码、测试各阶段的提示词、产出和人工修订。
4. 自己编写的代码、测试结果和界面截图。
5. 对 AI 优势、错误、局限和验证方法的个人反思。

## 10. 主要风险与预案

| 风险 | 预警信号 | 预案 | 负责人 |
|---|---|---|---|
| AI API 不可用 | 超时、余额不足、网络受限 | 核心评分坚持规则引擎，AI 建议自动降级 | B/C |
| 接口反复变化 | 联调频繁报字段错误 | 第 2 天冻结契约，变更须双端确认 | A |
| 匹配分不可信 | 同输入分数波动、无法解释 | 固定算法版本，返回分项与命中证据 | C |
| 隐私泄露 | 仓库出现真实简历/密钥 | 仅放脱敏样例，日志脱敏，提交前扫描 | A/B |
| 最后一天无法运行 | 仅在开发者电脑成功 | 第 5 天完成冷启动演练 | A/D |
| 视频超时或演示卡顿 | 试录超过 10 分钟 | 脚本控制 8-9 分钟，准备固定样例 | D |
| 个人报告同质化 | 四份报告内容几乎相同 | 共用背景事实，实施/问题/反思独立撰写 | 全员 |

## 11. 立即开始的 90 分钟清单

1. 0-15 分钟：填入 A-D 姓名，确定组长和代码仓库。
2. 15-30 分钟：全员确认 P0/P1/P2，不再扩大范围。
3. 30-50 分钟：C 给出技能词典 v0；B 给出简历 schema v0。
4. 50-65 分钟：A 定稿 API JSON；D 根据 JSON 建结果页 mock。
5. 65-80 分钟：确定 3 份简历和 5 份 JD 的脱敏样例。
6. 80-90 分钟：提交各自第一笔 commit，约定下一次集成时间。

